# Makefile for building the 'school' executable

# Default rule
all: school

# Link source files into the executable
school:
	gcc main.c school.c -o school

# Clean rule to remove the executable
clean:
	rm -f school
